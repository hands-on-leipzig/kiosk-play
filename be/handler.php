<?php

function register_screen($event_id) {
    echo "registering screen for event $event_id ...<br>";
    echo "done!";
}

function push_slides($event_id, $screen_id) {
    echo "pushing slides in event $event_id onto screen $screen_id...<br>";
    echo "done!";
}

function save_slides($event_id) {
    echo "saving slides for event $event_id ...<br>";
    echo "done!";
}